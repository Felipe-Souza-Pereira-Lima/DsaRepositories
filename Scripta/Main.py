import socket
from time import sleep

__version__ = '1.0.4'

if __name__ == '__main__':
    def __main__(maincode):
        keywords = []
        result = None
        for key in keywords:
            if maincode.startswith(key):
                result = maincode.replace(key, '')
        if result is not None:
            return result
        else:
            result = False
            return result
        return result

